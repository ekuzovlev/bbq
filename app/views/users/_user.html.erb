<span class="user">
  <span class="user-avatar">
    <%= image_pack_tag(user_avatar(user), class: 'img-icon', size: '16x16') %>
  </span>
  <span class="user-name">
    <% if request.path == user_path(user) %>
      <%= user.name %>
    <% else %>
      <%= link_to user.name, user_path(user) %>
    <% end %>
  </span>
</span>
